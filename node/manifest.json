{
  "manifest_version": "0.3",
  "name": "binomen",
  "display_name": "binomen — biological name checker",
  "version": "0.2.0",
  "description": "Checks whether an organism name is current, what it was called before, and whether authorities disagree.",
  "long_description": "Biological names are not stable identifiers. *Clostridium difficile* is now *Clostridioides difficile*; *Enterobacter aerogenes* is now *Klebsiella aerogenes*; *Lactobacillus* was split into about 25 genera. Both old and new names are heavily represented in the literature, so a name being familiar is no evidence that it is current — and a search on the current name alone silently misses everything published under the old one, returning few results rather than an error.\n\nbinomen answers four questions from a local index, with no network access at query time:\n\n- **Is this name current?** — and if not, what replaced it\n- **What else has this organism been called?**\n- **What should I search** to find the older literature\n- **Do authorities disagree?** — some synonymies are genuinely disputed, and it says so rather than picking\n\nEvery answer carries the NCBI Taxonomy release it came from. Nothing is generated by a language model. On first use it downloads a name index of about 46 MB, and checks for a newer one every couple of weeks.",
  "author": { "name": "Zachary Ruhe" },
  "license": "MIT",
  "keywords": ["biology", "taxonomy", "nomenclature", "literature-search", "microbiology"],
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "BINOMEN_DATA_DIR": "${user_config.index_location}"
      }
    }
  },
  "tools": [
    { "name": "check_name", "description": "Is there anything about this name that needs a closer look? Cheap; call it on any organism name." },
    { "name": "resolve_name", "description": "Accepted name, authorship, and identifiers for a name." },
    { "name": "get_synonyms", "description": "Every name recorded for a taxon." },
    { "name": "expand_query", "description": "Search terms covering a taxon's full naming history." }
  ],
  "user_config": {
    "index_location": {
      "type": "directory",
      "title": "Where to keep the name index",
      "description": "The name index is about 123 MB on disk. Leave the default unless you need it on another drive. It is kept outside the extension so that updating the extension does not re-download it.",
      "required": false,
      "multiple": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": { "node": ">=22.5.0" }
  }
}
