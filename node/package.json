{
  "name": "binomen",
  "version": "0.3.0",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "description": "binomen MCP server, field edition. No dependencies by design.",
  "scripts": { "test": "node --test --no-warnings" },
  "dependencies": {}
}
